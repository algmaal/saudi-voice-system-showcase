# نظام المساعد الصوتي السعودي - عرض توضيحي (Showcase)

هذا المشروع هو عبارة عن تطبيق ويب متكامل يهدف لاستعراض قدرات الذكاء الاصطناعي في التعامل مع اللهجة السعودية، سواء من خلال الدردشة النصية أو الأوامر الصوتية.

## 1. تقنيات المشروع (Technology Stack)

### الواجهة الأمامية (Frontend)
- **React**: لبناء واجهة المستخدم التفاعلية.
- **Vite**: كأداة بناء وتطوير سريعة.
- **Tailwind CSS**: لتنسيق الصفحات بشكل عصري ومتجاوب.
- **Radix UI / shadcn/ui**: مكتبة مكونات جاهزة لواجهة مستخدم احترافية.
- **Lucide React**: للأيقونات.

### الواجهة الخلفية (Backend)
- **Node.js & Express**: الخادم الرئيسي للتطبيق.
- **tRPC**: لبناء واجهات برمجية (APIs) آمنة ومتكاملة النوع (Type-safe) بين العميل والخادم.
- **OpenAI SDK**: للتواصل مع نماذج الذكاء الاصطناعي (مثل GPT-4o و Whisper).

### قاعدة البيانات (Database)
- **MySQL**: كنظام إدارة قواعد البيانات.
- **Drizzle ORM**: للتعامل مع قاعدة البيانات بطريقة حديثة وآمنة.

### التوثيق والتحقق (Auth & Validation)
- **Zod**: للتحقق من صحة البيانات.
- **OAuth**: لنظام تسجيل الدخول (يدعم OpenID).

## 2. هيكلة المشروع

- **`client/`**: يحتوي على كود الواجهة الأمامية (الصفحات، المكونات، الستايلات).
  - `pages/Home.tsx`: الصفحة الرئيسية التي تعرض الأقسام المختلفة.
  - `components/VoiceTestSection.tsx`: قسم تجربة الصوت (حالياً واجهة فقط).
  - `components/AIChatBox.tsx`: صندوق الدردشة الذكي المتصل بـ GPT-4o.

- **`server/`**: يحتوي على كود الخادم (Backend).
  - `_core/chat.ts`: إعدادات الدردشة والتعامل مع OpenAI.
  - `_core/voiceTranscription.ts`: خدمة تحويل الصوت إلى نص (تدعم العربية).
  - `_core/index.ts`: نقطة انطلاق الخادم.

- **`drizzle/`**: ملفات تعريف قاعدة البيانات (Schema) والتهجير (Migrations).

## 3. الميزات الرئيسية

1. **المساعد الصوتي (Voice Assistant)**:
   - يوجد نظام لتحويل الصوت إلى نص (Speech-to-Text) باستخدام تقنية Whisper، مصمم لدعم اللهجة السعودية واللغة العربية بشكل عام.
   - **ملاحظة**: الواجهة الحالية في `VoiceTestSection` هي نموذج تجريبي (Mock) وتعرض رسالة "سيتم إضافة المعالجة الفعلية قريباً"، لكن الكود الفعلي للمعالجة موجود في `server/_core/voiceTranscription.ts`.

2. **الدردشة الذكية (AI Chat)**:
   - يحتوي على نظام دردشة متكامل يدعم تدفق المحادثة (Streaming) واستدعاء الأدوات (Tool Calling) مثل معرفة الطقس أو إجراء الحسابات.

3. **التصميم (UI/UX)**:
   - تصميم عصري ونظيف يدعم اللغة العربية (RTL) بشكل ممتاز.
   - يستخدم خطوط "Poppins" و "Inter" (وقد يحتاج لإضافة خط عربي مخصص مثل "Cairo" أو "IBM Plex Sans Arabic" لتجربة أفضل).

## 4. حالة التشغيل

تم إنشاء هذا الملف تلقائياً لتسجيل حالة المشروع.
