# نظام المساعد الصوتي السعودي - عرض توضيحي (Showcase)

هذا المشروع هو عبارة عن تطبيق ويب متكامل يهدف لاستعراض قدرات الذكاء الاصطناعي في التعامل مع اللهجة السعودية، سواء من خلال الدردشة النصية أو الأوامر الصوتية، مع التركيز الخاص على أنظمة خدمة العملاء الآلية (Call Center).

## 1. تقنيات المشروع (Technology Stack)

### الواجهة الأمامية (Frontend)
- **React**: لبناء واجهة المستخدم التفاعلية.
- **Vite**: كأداة بناء وتطوير سريعة.
- **Tailwind CSS**: لتنسيق الصفحات بشكل عصري ومتجاوب.
- **Radix UI / shadcn/ui**: مكتبة مكونات واجهة المستخدم.
- **Lucide React**: للأيقونات.
- **Web Audio API**: للمعالجة الصوتية في المتصفح وكشف النشاط الصوتي (VAD).

### الواجهة الخلفية (Backend)
- **Node.js & Express**: الخادم الرئيسي للتطبيق.
- **tRPC**: للتواصل بين العميل والخادم بشكل آمن ومتكامل النوع.
- **Hugging Face Inference API**: لاستخدام أحدث موديلات الذكاء الاصطناعي مفتوحة المصدر.
- **Drizzle ORM & MySQL**: لإدارة قاعدة بيانات المشروع.

### موديلات الذكاء الاصطناعي (AI Models)
- **STT (Speech-to-Text)**: موديل **Whisper-large-v3** لتحويل الصوت إلى نص بدقة عالية للهجات العربية.
- **LLM (Language Model)**: موديل **ALLaM-7B-Instruct-preview** لإدارة الحوار، استخراج البيانات، والتحقق من صحتها.
- **TTS (Text-to-Speech)**: موديل **NAMAA-Saudi-TTS** لإنتاج صوت طبيعي جداً باللهجة السعودية.

## 2. ميزات الكول سنتر الذكي (Smart Call Center)

يعد نظام الكول سنتر هو الإضافة الأحدث والأكثر تطوراً في المشروع، ويتميز بـ:
1. **إدارة الحوار الآلية**: محرك ذكي يدير المحادثة بمرونة، يسأل الأسئلة، يستخرج الإجابات، ويتأكد من دقتها.
2. **كشف النشاط الصوتي (VAD)**: التبديل التلقائي بين دور الوكيل ودور العميل دون الحاجة لضغط أزرار يدوية.
3. **ذكاء الاستخراج**: استخراج البيانات (الاسم، العمر، الموقع، الوظيفة) حتى لو قيلت ضمن سياق كلام طويل أو باللهجة السعودية.
4. **تأكيد البيانات**: نظام تحقق فريد يراجع البيانات مع العميل قبل حفظها (مثلاً: "فهمت إن عمرك 25 سنة، صح كذا؟").
5. **معالجة المقاطعات**: القدرة على التعامل مع أسئلة العميل الخارجة عن السياق أو المقاطعات المفاجئة وإعادته لمسار المحادثة.

## 3. هيكلة المشروع المحدثة

- **`client/src/`**: 
  - `pages/CallCenter.tsx`: الصفحة الرئيسية لنظام الكول سنتر.
  - `components/CallCenterSection.tsx`: المكون المتطور الذي يدير حالة المكالمة والصوت والـ VAD.
  - `components/VoiceTestSection.tsx`: قسم اختبار الصوت الفوري.
  - `components/AIChatBox.tsx`: حوار نصي ذكي مع GPT-4o.

- **`server/`**: 
  - `callCenter.ts`: المحرك الرئيسي لإدارة منطق الحوار (State Machine) واستخراج البيانات عبر LLM.
  - `audioProcessing.ts`: خدمات معالجة الصوت (transcribe, synthesize) وربطها بـ Hugging Face و NAMAA.
  - `callCenterRouter.ts`: نقاط الاتصال (API) لنظام الكول سنتر.
  - `_core/voiceTranscription.ts`: معالجة الملفات الصوتية العامة.

## 4. البنية التحتية والتشغيل

- **الاستضافة**: مهيأ للعمل على منصة **Netlify** مع دعم كامل لـ Environment Variables.
- **قاعدة البيانات**: نظام MySQL مع دعم كامل لعمليات الهجرة عبر Drizzle.
- **الأداء**: استخدام نظام SSE (Server-Sent Events) لضمان استجابة سريعة من موديلات الصوت الثقيلة.
- **الأمان**: جميع مفاتيح الـ API مخزنة بشكل آمن في ملفات البيئة (`.env`).

---
*تم تحديث هذا الملف بتاريخ 16 فبراير 2026 ليعكس الحالة الفعلية للأنظمة الصوتية المتطورة المضافة للمشروع.*
