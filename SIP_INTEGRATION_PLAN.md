# خطة ربط الكول سنتر بخط هاتف سعودي (SIP Integration)

> ⏳ **الحالة**: مؤجل للتنفيذ لاحقاً

## الهدف
تمكين نظام الكول سنتر الذكي من **إجراء مكالمات صادرة فقط** عبر رقم سعودي (+966) باستخدام بروتوكول SIP.

## كيف يعمل النظام
1. يتم طلب API لبدء المكالمة (`startOutboundCall`) مع رقم العميل.
2. النظام يتصل بالعميل عبر مزود الخدمة (SIP Provider).
3. عند رد العميل، يبدأ المساعد الذكي بالحديث وجمع البيانات تلقائياً.

## مزودي الخدمة المتاحين (أرقام سعودية)

| المزود | النوع | ملاحظات |
|--------|-------|---------|
| **Unifonic (يونيفونك)** | Voice API + SIP | الأفضل - شركة سعودية، أرقام محلية، API برمجي |
| **Cequens (سيكوينز)** | Voice API + SIP | خيار قوي، مكتب في الرياض |
| **3CX + STC/Mobily** | PBX + SIP Trunk | 3CX هو البرنامج، تحتاج شراء رقم من STC أو Mobily |
| **AstraQom** | SIP Trunk | أرقام سعودية افتراضية |

> **ملاحظة**: 3CX هو نظام إدارة مكالمات (PBX) وليس مزود أرقام. لاستخدامه تحتاج ربطه بمزود SIP Trunk مثل STC أو Mobily للحصول على رقم سعودي.

## المتطلبات التقنية

### مكتبات Node.js المطلوبة
- `sip.js` — للتواصل عبر بروتوكول SIP
- `ws` — WebSocket polyfill
- `wrtc` — WebRTC لمعالجة الصوت (RTP)

### ملفات جديدة
- `server/_core/sipGateway.ts` — خدمة SIP Gateway للمكالمات الصادرة

### متغيرات البيئة (.env)
```
SIP_URI=sip:100@your-provider.com
SIP_PASSWORD=your_password
SIP_WSS_URL=wss://your-provider.com:5001/sip
SIP_OUTBOUND_PREFIX=9
```

### API Endpoint جديد
- `callCenter.startOutboundCall` — يبدأ مكالمة صادرة لرقم محدد

## تدفق المكالمة

```
Admin → API: startOutboundCall(+966XXXXXXXXX)
    ↓
Bot → SIP Provider: INVITE (اتصال)
    ↓
SIP Provider → العميل: رنين...
    ↓
العميل يرد → المكالمة تبدأ
    ↓
┌─────────── حلقة المحادثة ───────────┐
│  Bot يتكلم (TTS/NAMAA) → العميل     │
│  العميل يتكلم → Bot يسمع (Whisper)  │
│  Bot يحلل (ALLaM) → يرد             │
└──────────────────────────────────────┘
    ↓
المكالمة تنتهي → البيانات محفوظة
```

## خطوات التنفيذ
1. [ ] اختيار مزود الخدمة والتسجيل للحصول على حساب ورقم سعودي
2. [ ] تثبيت المكتبات (`sip.js`, `ws`, `wrtc`)
3. [ ] إنشاء `sipGateway.ts` للربط مع مزود الخدمة
4. [ ] إضافة API endpoint `startOutboundCall`
5. [ ] إنشاء واجهة Admin بسيطة لإدارة المكالمات
6. [ ] اختبار مع رقم حقيقي

---
*تم إنشاء هذا الملف بتاريخ 21 فبراير 2026*
